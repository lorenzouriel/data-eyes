apiVersion: 1

datasources:
  - name: ${DS_MSSQL}
    type: mssql
    uid: sqlserver_ds    
    access: proxy
    url: ${DB_HOST}
    user: ${DB_USER}
    secureJsonData:
      password: ${DB_PASSWORD}
    database: ${DB_NAME}  
    isDefault: false
    editable: true