# Local dev only: the Vite dev server (:5173) and the FastAPI backend run on
# different origins, so the frontend needs to know where to send API calls.
# In production the frontend is served behind the same reverse proxy as the
# backend (see nginx.conf), so this is left unset there (same-origin, "").
VITE_API_BASE_URL=http://localhost:8090
